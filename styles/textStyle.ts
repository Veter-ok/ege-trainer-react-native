import { StyleProp, TextStyle } from "react-native";

export const basicTextStyle:StyleProp<TextStyle> = {
    color: "#fff",
    fontSize: 16,
    textAlign: "center",
    marginBottom: "auto",
    marginTop: "auto",
}